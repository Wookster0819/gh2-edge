{
  "version": 2,
  "installCommand": "npm install --omit=dev --no-audit --no-fund --no-package-lock",
  "buildCommand": "",
  "framework": null,
  "routes": [
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
